import java.util.List;
import java.util.Map;

/**
 * Created by faiter on 10/18/17.
 */
public class Stats {

    public static Map<Letter, Integer> frequency(List<Letter> letters){


        /*Map<Letter, List<Letter>> collect = letters.stream().collect(Collectors.collectingAndThen(Collectors.groupingBy(o -> o)));

        List<Integer> ints = collect.entrySet().stream().map(letterListEntry -> letterListEntry.getValue().size()).collect(Collectors.toList());

        Map<Letter, Integer> out = new HashMap<>();

        for (int i = 0; i < collect.size(); i++) {


        }

        return out;*/
return null;
    }
}
